"use client";

import { Icon } from "@iconify/react";

const LockIcon = (props: { className: string }) => {
  return <Icon icon="teenyicons:lock-solid" className={props.className} />;
};

export default LockIcon;
